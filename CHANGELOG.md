# Changelog

所有重要变更都会记录在这里。

## [1.0.0] - 2026-08-31

### Changed

- 调整为仅支持可信自建 Nitter：不再提供或维护公共实例默认值、公共主机模式和 Anubis/Poast 挑战解算。
- `instances` 成为唯一实例配置，同时用于用户 RSS/HTML、搜索、List 和后台并发抓取；Blogger 固定 RSS 优先、HTML 自动后备。
- 删除 `search_instances`、`blogger_html_instances`、`concurrent_fetch_instances` 和 `user_html_fallback`。旧实例字段不会读取、迁移或写回，启动日志会列出被忽略的 origin。
- 网络层允许合法 HTTP(S) 的 loopback、Docker 服务名、私网 IP、媒体 URL 和重定向；继续保留 URL 协议/语法、超时、响应/媒体大小、数量和时长限制。
- Dashboard “镜像测试”重做为统一实例能力测试，一次报告用户 RSS、用户 HTML、搜索和可选 List 结果。
- 文档增加容器内地址选择和可信来源提醒：插件会访问实例响应中的媒体与重定向，部署者应通过 Docker network、防火墙和反向代理隔离内部管理面。

## [0.18.6] - 2026-08-17

### Added

- 新增统一的结构化任务摘要日志，覆盖手动查询、搜索、被动链接解析和后台分组检查。
- 新增 HTML 门禁检测回归覆盖，明确支持 Anubis/Poast 挑战并优先保留真实时间线内容。

### Changed

- 后台日志现在统一记录来源、订阅类型、实际生效实例、轮换轨迹、抓取/水位状态、发送统计、warning/error 和耗时；简略模式也会保留关键告警。
- 手动查询和搜索日志改用实际发送数量及真实结果状态，不再把“调用完成”直接记为完整成功。
- 所有 RSS、HTML、状态解析、xdown、图片和视频请求统一使用插件内置的浏览器 User-Agent 与语言请求头；协议所需的 `Accept`、`Referer`、`Origin` 等字段继续按请求保留。
- 移除 `user_agent` 与 `media_user_agent` 配置项。旧配置会在启动迁移时清理并保存，后续由插件统一管理请求头。
- 临时网络探测和一次性脚本统一归档到 `testignore/`，默认 pytest 仅收集 `tests/`。

### Fixed

- 修复当前 Nitter 实例改写出的 `/status/...` 等同站链接混入推文正文或译文的问题；普通外部链接仍按原规则保留。
- 修复 HTML 响应中的显式 Anubis/Poast 挑战及错误面板与占位 timeline 节点混杂时被误判为正常页面、绕过门禁解算的问题；无明确挑战结构时真实时间线内容继续优先于通用门禁文案。
- 修复结构化日志在跳过、部分发送、无效目标、媒体待重试或基准重建失败时丢失关键告警的问题。
- 修复日志诊断字段可能携带完整私有 URL 或敏感内容的问题，统一经过安全清理。

### Docs

- README 增加 [nitter-installer](https://github.com/shitianyaa/nitter-installer) 一键部署自建 Nitter 的入口、命令、接入方式和远程脚本安全提示。
- 明确“静态文件约 10 MB”仅是一次测试中只统计脚本和静态配置文件的观测值，不等于 Docker 化部署的完整磁盘或内存占用，并补充 VPS 自查命令。
- 更新实例、List、日志、正文链接、测试和维护文档，使配置边界、门禁回退和诊断字段与当前实现一致。

### Tests

- 增加结构化日志、HTML 门禁、来源链接清理和统一请求头的行为测试；移除只验证实现细节的无功能测试。

## [0.18.5] - 2026-08-11

### Added

- 新增 QQ Official 官方 Bot 平台支持：通过独立适配器发送普通消息，不误用私人号 OneBot 合并转发或 raw action。同时覆盖 `qq_official`（WebSocket）和 `qq_official_webhook`（Webhook）两种官方适配器。
- README 增加 QQ Official 实际 Markdown 推送效果截图。

### Docs

- README 增加猫猫计数的浏览量图片，并补充计数服务来源。

### Changed

- QQ Official 正文 Event/UMO 使用安全转义的官方 Markdown；包含媒体时先发正文，再独立发送图片/视频，不把 Markdown 与 `media` 混在同一条消息中。
- QQ Official UMO 主动推送直接调用官方群聊/C2C 消息接口；Markdown 被拒时自动用 `msg_type=0` 纯文本重发，不依赖运行期群场景缓存。
- 最低 AstrBot 版本提高到 4.26.0，以使用 QQ Official 群 UMO 主动发送实现。
- 支持平台列表只展示规范平台名；内部 OneBot/QQ Official 兼容别名仍保留在路由层。

### Fixed

- QQ Official Markdown 转义现在会处理带缩进的行首标记：推文正文里缩进的 `-`、`>`、`#`、`1.` 后的空格此前漏转义，会被 QQ 解析成嵌套列表导致排版错乱。
- OneBot 合并转发的图片节点不再重复显示作者、`图片附件` 和 Nitter 来源；引用原文中的空行也不再生成额外的 `>` 标记。
- QQ Official 媒体-only 推送只有在媒体完整送达时才记录推文送达 ID，避免历史记录把不完整的媒体推送标记为已送达。

## [0.18.4] - 2026-08-03

### Added

- 新增按推送目标（完整 UMO）共享的作者黑名单：可用管理员命令或 Dashboard 添加、删除、查看；同一目标跨多个订阅分组通用，发送阶段按目标过滤。

### Fixed

- 修复命令和 Dashboard 并发维护同一推送目标作者黑名单时可能互相覆盖的问题，统一使用共享锁串行化读改写和失败回滚。
- 修复作者黑名单解析异常会中断定时推送或 `/推文状态` 的问题；异常时记录日志并保留其他目标的处理机会。
- 修复推送目标发送异常时 seen/水位不推进、导致已成功目标在后续周期重复推送的问题；失败目标本轮直接标记为已处理，失败仍不计入成功数并保留日志。
- 定时发送失败现在会写入 `failed` 推送历史，Dashboard 会显示失败原因并支持从历史记录手动重推。
- 修复正文已送达但媒体发送失败、且发送器未返回逐推文送达 ID 时被误记为完全失败的问题，历史状态继续保留为 `partial_failed`。
- 新增版本一致性测试，防止 README 徽章、插件注册、metadata 和 CHANGELOG 的发布版本不一致。
- 补充独立常见问题文档，明确目标黑名单、分组历史重推、共享 seen、发送失败终态和 Tag/List 基准重建边界。

## [0.18.3] - 2026-08-02

### Fixed

- 修复 Tag 搜索首屏达到 20 条后提前停止分页的问题；Tag/List 现在都会在 `html_max_pages` 范围内寻找旧扫描基准，扫描未完整时按 `max_tweets_per_check` 自动处理并在安全条件满足后重建当前第一页基准。
- 扫描未完整时不再使用 List 专属的“本轮不发送”逻辑：上限为 `0` 时跳过推送并重建基准，正数按上限推送成功后再重建；发送失败、基准无效或写入失败时保留旧水位，并使用通用日志提示。
- 改进 Tag/List 检查结果的可读性：区分正常无更新、空结果、过滤后为空、扫描不完整和抓取失败，显示实例轮换结果与水位状态，避免把实例不可用误解为“没有新推文”。
- Tag/List 首轮抓取为空不再计入「抓取失败」，改记为「返回空结果」；日志摘要的 `failed=` 计数和推送消息中的失败列表相应变化，行为（不初始化、下轮重试）保持不变。
- 镜像请求失败改用带状态的 `HtmlFetchError`，实例结果不再依赖异常文案子串匹配；网关返回异常页面现在显示「异常页面」而非 `RuntimeError`。

## [0.18.2] - 2026-08-02

### Fixed

- 修复已初始化 List 订阅的本轮扫描在前 20 条处截断并提前推进水位的问题：现在会继续翻页到命中旧水位或游标结束；达到 `html_max_pages` 仍未完成且第一页有有效状态 ID 时，自动用第一页最多 20 个状态 ID 重建基线，本轮不发送并提示旧积压可能被跳过。若第一页没有有效状态 ID，或 List 尚未初始化，则保留旧基线并按分页失败处理。首次基线和后续持久化水位仍最多保存 20 个 ID。
- 修复 List 基线重建的持久化顺序与一致性：统一事务路径原子更新 seen 与扫描水位，兼容旧存储适配器时按 seen 后水位顺序回退，并在写入失败时保留旧状态。

## [0.18.1] - 2026-07-29

### Fixed

- 修复已初始化 List 订阅的本轮扫描在前 20 条处截断并提前推进水位的问题：现在会继续翻页到命中旧水位或游标结束；达到 `html_max_pages` 仍未完成时跳过本轮，不写 seen、不推进水位。首次基线和后续持久化水位仍最多保存 20 个 ID。
- 修复自定义 Telegram 实例 ID（如 `tg-main`）遮蔽平台名称/类型，导致 Telegram 专用发送能力识别失败的问题。
- 修复同一 UMO 中相同 status 链接并发解析时可能重复 resolve、重复发送的问题；失败后仍允许重试。
- List ID 后端校验改为正 `uint64`，拒绝超过 `18446744073709551615` 的 20 位数字。
- 翻译流程会在模型/provider 解析前跳过空正文及“无正文”占位符，避免媒体推文触发无效的大模型翻译。
- 媒体直链下载/时长探测不再对 `video.twimg.com`、`pbs.twimg.com` 等使用 `Referer: https://xdown.app/`（会触发 CDN HTTP 403）；改为 `https://x.com/`。xdown API 请求仍使用 xdown 站点头。

### Changed

- Ruff 与主分支保持一致为 `0.15.22`，移除仅供 0.16 使用的项目级 `ruff.toml`。
- README 精简为上手与定位（约减半篇幅）；细则、行为边界与诊断统一指向 `docs/advanced.md` 等专题文档。
- Telegram 推文消息保留可点击的 X 链接，但关闭网页预览，避免视频推文卡片重复显示作者和 HLS 播放文案。
- 推文正文布局（R1，含 QQ plain 与 Telegram）：有译文时译文作主文、原文用 `>` 引用块；去掉「翻译」「原文」小标题。Telegram 使用 `@作者 · [🔗 查看推文](链接)` 头。
- 纯媒体/空正文：Fx `text=""` 不再误用 `raw_text` dict；只保留头/作者行 + 媒体摘要，不显示「（无正文）」。
- 推文展示时间统一为 **Asia/Shanghai（UTC+8）** `YYYY-MM-DD HH:MM:SS`：覆盖 RSS、链接解析（Fx/Vx/Syn）、HTML 搜索/List（Nitter tweet-date），渲染层再兜底一次。

## [0.18.0] - 2026-07-28

### Added

- 新增可选 `auto_parse_tweet_links_enabled`（默认关闭）：被动识别聊天中的公开 X/Twitter status 链接，解析正文与媒体并发送；无新命令；忽略 Bot 自身消息；翻译与「有译文时显示原文」跟随全局；不受订阅/冷却/seen/全局图视频开关限制。
- 新增 List 分组完整支持：Dashboard 可创建、编辑、保存和校验纯数字 List ID；概览、诊断、历史和批次摘要分别使用 List 术语，List 定时抓取复用 `search_instances`。
- 文档补充「把关注快速导入 Public List」：推荐第三方扩展 [X Follow to List](https://github.com/DrErwin/X-Follow-to-List) 批量把 Following 加入 X List，再把 Public List ID 填入本插件；并说明 X 官方加入 List 约 24 小时内大约 100 次量级的实践限额（随账号与平台策略波动）。见 README 与 `docs/twitter-lists.md`。
- Dashboard 镜像测试按当前模式读取实际运行配置；URL 留空时按配置顺序串行测试全部 RSS 或搜索实例，返回逐站结果、推文数、耗时和汇总。
- 调度存储初始化改为可等待且并发安全；启动首检覆盖仅每日、仅间隔和无定时槽位的启用分组，并记录首检/手动检查的完整耗时与结果日志。

### Fixed

- 修复启动首检被间隔/每日槽位初始化吞掉的问题，并在首检完成后锚定槽位，避免同一轮重复检查。
- 修复 Tag/List 结果、失败摘要、推送历史和 OneBot 节点暴露 `q:`、`list:` 内部键或 Blogger 专用文案的问题。
- 移除 `/推文状态`、`/推文检查` 用户可见的索引统计；SQLite seen 和去重机制保持不变。

### Changed

- 统一批次摘要量词：Blogger 为“n 位博主”、Tag 为“n 个搜索订阅”、List 为“n 个 List”。
- Blogger、Tag、List 后台检查统一采用转发过滤双层开关：全局 `filter_reposts_enabled` 与分组同名开关同时开启才过滤；新分组和旧分组缺省值均为开启。

## [0.17.1] - 2026-07-27

### Added

- 分组配置 `max_tweets_per_check`：单个账号/查询单次检查最多推送的推文条数，0 表示不限制。适用于爆发式更新场景，避免一次推送过多消息。
- 全局重试机制：`max_global_retries`（默认 2）、`retry_delay_base`（默认 5.0s）、`retry_delay_on_cooldown`（默认 10.0s）。所有实例失败后延迟重试，渐进式延迟（5s → 10s → 15s），智能跳过验证错误和 probe 模式。
- 默认搜索实例扩展为三镜像：`tiekoetter.com`、`poast.org`、`kareem.one`（CF 验证通过，3x 冗余 + 自动门禁解算）。
- 新增实例配置完整指南：`docs/instances-guide.md` 详细说明 RSS vs HTML 双路架构、默认实例配置、容错机制、错误提示对照表和最佳实践。

### Fixed

- 标签查询间隔：Tag 分组查询现在应用 `send_user_interval` 配置，避免连续快速请求导致 429。
- 标签查询日志：增强 `_fetch_group_query` 方法的日志输出，显示查询开始、成功（实例、推文数）和失败（错误类型、详情）。
- Tag 分组推送间隔：`send_target_interval` 和 `send_user_interval` 现在支持分组级别配置覆盖。
- 推文检查文案：统一检查摘要术语，避免把去重状态误解为订阅数量。
- 重试机制：改为"轮换优先"策略——轮流尝试所有实例，重复 N 轮，而非在单个实例上重试 N 次后才换下一个。例如 3 个实例、2 轮：A→B→C→A→B→C，而非 A×2→B×2→C×2。更快找到可用实例。

### Changed

- 默认实例：博主 RSS 默认 `nitter.net`（可多站）；搜索默认 `tiekoetter.com` + `poast.org` + `kareem.one` 三镜像。移除配置项 `blogger_html_instances`；博主不再做公共 HTML 回退池，避免与标签搜索抢资源。`user_html_fallback` 默认关闭且隐藏。
- 错误提示优化：区分配置错误（`未配置 Nitter 实例`）和运行时故障（`本轮所有 Nitter 实例均不可用`），明确失败原因。
- 重试策略升级：采用"实例轮换 + 全局重试"双层机制。第一层立即轮换所有实例（ready 按成功率排序优先，cooling 殿后），全部失败后进入第二层延迟重试（渐进式 5s/10s/15s），显著提升网络抖动下的容错能力。
- 文档完善：README、`docs/advanced.md` 和 `docs/instances-guide.md` 补充 RSS vs HTML 架构说明、默认实例配置、`user_html_fallback` 回退机制、全局重试参数和错误提示对照表。
- 镜像选择：按请求成功率内存记分（有结果满分成功、空结果 soft、失败降权；HTML transport 异常也记失败），博主 RSS 与搜索 HTML 分账；ready 高分优先，冷却殿后；不写盘。
- 日志：`brief_log_enabled`（默认开）同时收敛 HTML 搜索/回退过程日志；`session load` 全抑制，gate 同类去重，try/冷却/空页过程行在简略模式下不刷屏；失败、punish、轮换成功/全空摘要仍保留。
- 修复 HTML 搜索/用户页：全部镜像 HTTP 成功但结果为空（含整页纯转推被滤掉）时返回空列表，不再 `HTML search failed`；标签定时可正确走「首次空结果不 init seen」，避免一直记失败、永远不推。
- 手动命令：新增 `manual_send_interval`，非合并转发时逐条消息间隔（默认 0）；在平台适配前 sleep，多平台生效。
- AI 翻译：全局 `show_original_when_translated`（默认 true）；关闭后有译文时隐藏原文；分组 `hide_original_when_translated` 可在全局显示时再隐藏。
- HTML 搜索/用户页：多镜像失败后轮换下一实例（ready 优先按成功率排序，冷却实例殿后）。
- `watch_queries` 落盘改为纯字符串列表，避免 AstrBot WebUI 把对象显示成 `[object Object]`；兼容读取旧 `{query,type}`，并丢弃损坏的 `[object Object]` 项。
- 文档：标签定时获取/发送数量（固定约 20、滤 RT/seen、新帖全发）；配置示例改为字符串列表。
- 标签分组增加风险提示：使用私人 QQ 号作为 Bot 时不建议启用标签定时。
- 转推过滤：`/推文`/`/镜像测试` 固定不过滤；`/推文搜索` 与标签分组定时固定过滤纯转推（无配置项）；博主分组仍用 filter_reposts_enabled。
- OneBot 合并转发遇到 retcode 1200 / res_id 失败时，自动拆成更小段重试；仅对未送达段降级直发（再失败则纯文本），避免半成功整包重复发送。
- 配置文案：去掉 send_video_attachments「不太成熟/还在优化」表述，改为默认关闭与能力说明。
- 修复发送层 hide_original/link_style 接线：adapter 接受 hide；UMO 直发/合并转发关键字透传；避免 link_style 位置参数错位导致 Telegram markdown 丢失。
- 推文布局：Telegram 首行改为 [@作者](链接)；`omit_status_url` 开启时剥离正文/译文内嵌 URL，关闭时保留；空正文占位；有译文时翻译块在原文前。
- 分组开关 hide_original_when_translated：有 AI 译文时只发翻译、隐藏原文；无译文仍发原文。修复搜索会话缓冲假空文案，缓存命中不消耗冷却。
- 手动 /推文搜索：按会话缓存整页搜索结果，按请求条数发放，不足再翻页拉取；同会话同查询约 10 分钟内不重复已展示推文，每查询最多缓存 40 条（默认开启，无额外配置）。
- 修复搜索结果作者显示为标签/查询词、正文为空，以及 Telegram 链接摘要误用查询词：渲染优先 tweet 链接作者与正文；HTML 解析增强 tweet-content 提取。
### Added

- 分组开关 `omit_status_url`（默认开启）：发送时去掉推文 URL 明文，并剥离正文/译文中的 http(s)；Telegram 使用摘要 Markdown 链到原文（`MessageChain.use_markdown`）。仅媒体模式仍不调用 AI 翻译。

## [0.17.0] - 2026-07-23

### Added

- Dashboard 镜像连通诊断支持博主 RSS / 博主 HTML / 搜索三种模式，并从配置同步实例列表；创建/保存分组后可正确选中与切换。
- Dashboard 文案按博主/标签分组区分（推送分组、无效搜索查询等）。
- AstrBot 配置 `tweet_groups` 拆成模板「博主分组 / 标签分组」；添加时先选类型。旧 `__template_key=group`（用户分组）启动迁移为 blogger。
- 新增 HTML 后端：`blogger_html_instances` / `search_instances`、博主 RSS 失败后的 HTML 用户页回退，以及非管理员命令 `/推文搜索`（标签带 `#`，短语不自动加 `#`）。
- 分组类型 `group_type`：`blogger`（`watch_users`）与 `tag`（`watch_queries`，`type=tag|phrase`）对位；标签分组定时搜索，seen 键为 `q:<casefold query>`；Dashboard 支持创建/编辑类型与查询列表。
- 管理命令 `/标签导入`、`/标签删除` 维护标签分组查询；`/订阅导入` `/订阅删除` 拒绝标签组以免写错字段。
- `/订阅导出 [分组]` 同时导出博主 `watch_users` 与标签 `watch_queries`，可按分组名过滤。
- 调度：首次抓取空结果不初始化 seen；HTML 路径支持按作者媒体过滤纯文本；状态汇总改用 `account_keys`。

### Fixed

- 第二刀：RSS 本轮检查/命令内跳过已失败镜像（不写盘）；`retry_attempts` / `retry_delay_seconds` 进 basic 配置（默认 2 / 5s）；HTML RateLimiter 线程锁；标签 watch_queries 规范化后回写配置。
- 修复标签分组 seen/watermark 无法落库：账号键 `q:...` 不再被用户名规范化丢弃（`normalize_seen_account_key`）。

- 修复后台检查只截取少量 RSS 结果导致更新较多时漏推的问题：固定扫描首屏约 20 条，首屏未出现上次基准时按 `Min-Id` 翻页到该基准，并在本轮发送全部未 seen 推文；移除不再生效的 `scheduled_fetch_limit` 配置。

### Changed

- 后台推送按 RSS 返回顺序发送；并发准备按完成顺序发送；聊天消息移除推文序号和账号进度，并在每个目标本轮第一条消息显示博主数、推文数和分组概括。
- 分组新增 `media_only_enabled`（WebUI 名称“仅媒体”）。有效时只发送作者和成功准备的附件；临时媒体失败下轮重试，明确策略跳过允许扫描基准推进。全局媒体不可用时仅在 WebUI 和日志提示并回退完整内容。

## [0.16.0] - 2026-07-21

### Removed

- 移除暂存定时发布：配置项、`/推文队列` / `/推文发布` 命令、WebUI 暂存队列页、SQLite `pending_tweets` / `pending_media` 表与 `cache/staged/` 暂存媒体流程。
- 移除非翻译 AI 能力：AI 摘要、智能过滤、改写、识图、AI 评论及相关配置、渲染与 WebUI 展示。
- 升级时删除旧 pending 表和 `cache/staged/` 媒体；独立的 0.16 缓存迁移标记确保清理不会被旧 marker 跳过，相关旧配置会被清理。

### Changed

- 后台检查发现新推文后统一即时推送；QQ/aiocqhttp 仍可按 `merge_tweet_threshold` 合并转发。
- AI 处理仅保留翻译；`metadata.yaml`、运维面板文案与 agent/项目文档同步为当前能力边界。
- 回补最小调度与平台发送回归：seen watermark、发送失败不写 seen、QQ 合并顺序、Telegram flood control。
- 清理删除后的死字段与兼容别名：`queued_tweets` / `pending_ids` / `image_caption` / `ai_comment`、`clear_non_staged_cache` 与 enrichment 日志参数。

## [0.15.0] - 2026-07-10

### Fixed

- 修复 `max_video_duration_minutes` 在分组配置界面修改后仍读取隐藏旧默认值的问题。

### Changed

- README、进阶说明和项目文档补充命令权限、推送历史存储、纯文本过滤字段及 AstrBot 兼容版本说明。
- 增加显式版本号一致性回归测试，避免 metadata、插件注册、README 徽章和 Changelog 再次错位。

## [0.14.0] - 2026-07-05

### Added

- 新增 AstrBot Plugin Pages 运维面板，可查看概览、分组订阅、暂存队列、最近推送、镜像测试和缓存清理；面板不替代 AstrBot 设置页，复杂配置仍在 `_conf_schema.json` 对应配置界面维护。
- WebUI `分组订阅` 页面改为左侧分组列表 + 右侧详情编辑面板，并新增自定义分组创建、保存和强制删除。
- WebUI 最近推送历史中的推文链接点击后会复制原推文链接并显示提示；分组 `group_id` 改为内部稳定键，创建和缺失迁移时由插件自动分配。
- 旧配置中已有的 `group_id` 会继续保留；显式旧 `global` 分组不再被强制改写为 `default`，缺失 ID 但旧运行时已用英文数字名称作为 ID 的分组会继承该名称。
- WebUI 最近推送新增失效分组检测，可列出已推送但当前配置不存在的 `group_id`，并在确认后清理该分组运行数据。
- WebUI 最近推送会标记主体已送达但媒体附件失败的历史记录，并保留错误摘要，便于手动重发。
- 新增插件图标，并在 README 顶部展示；图标致谢说明同步补充。

### Changed

- QQ/OneBot 直发和合并转发会将图片附件拆成独立消息或独立节点发送，降低图文同条发送导致的超时概率；非 QQ 平台保持原有图文同消息行为。
- QQ/OneBot 普通直发图片附件失败会重试一次；Lark/Feishu 媒体附件发送也会先重试，再按原有去视频降级规则处理。
- 后台检查改为以 seen 中最大的数字推文 ID 作为时间基准，后续翻页或过滤才发现的更旧未知推文只补入 seen，不再回填推送。
- 订阅导入、删除和导出命令补充操作日志，并调整返回文案，明确分组、账号数量、未改动原因和导出格式。
- 删除自定义分组时会同步清理对应的 SQLite 运行数据和 `cache/staged/<group_id>/` 暂存媒体目录。
- 重构代码结构：调度、WebUI API、配置、存储、AI、渲染、共享模型和发送编排分别收敛到 `scheduler/`、`plugin_api/`、`config/`、`storage/`、`ai/`、`rendering/`、`shared/` 和 `delivery/` 包，根目录仅保留插件入口和项目元信息。

## [0.12.0] - 2026-07-05

### Added

- 新增后台检查性能配置：可选启用账号 RSS 并发拉取、并发专用 Nitter 镜像池、媒体和模型并发准备；默认保持关闭。
- 升级到普通媒体发送后删除策略时，启动阶段会自动执行一次普通缓存清理；如果存在删除失败文件，下次启动会继续重试。

### Changed

- 后台并发拉取只使用 `concurrent_fetch_instances`，不回退普通 `instances`；专用池内每个镜像总尝试 3 次后再尝试下一个镜像。
- 启用后台并发拉取或并发准备后，发送、暂存入队和 seen 更新仍按 `watch_users` 与推文原顺序执行。
- 普通图片/视频缓存改为发送流程结束后立即删除，移除 `media_cache_retention_days` 配置；暂存发布媒体仍保留在 `cache/staged/` 并按暂存策略清理。
- `/推文` 和 `/镜像测试` 继续保留纯文本推文；后台纯文本过滤只按分组开关生效。

### Fixed

- 修复 Nitter 视频缩略图未被识别为作者媒体，导致后台纯文本过滤误过滤视频推文的问题。
- 修复纯文本统计把已过滤转发计入纯文本过滤数量的问题。
- 明确 Twitter Article 卡片封面和引用推文媒体不算当前作者上传媒体，避免后台误判。

## [0.11.0] - 2026-06-30

### Added

- 新增 `filter_reposts_enabled`，默认过滤博主转发他人的推文；手动 `/推文`、`/镜像测试` 和定时推送都会按 RSS item 主链接作者判断，博主自己发布的引用或评论推文仍会保留。
- 新增 `brief_log_enabled` 后台日志简略模式，默认只输出每轮检查/暂存发布结果摘要、失败详情、推送成功率和关键 warning/error。
- 新增 `scripts/probe_nitter_fetch.py`，可在本地复用插件抓取与转发过滤逻辑测试指定账号的 Nitter RSS 返回。
- 媒体设置新增 `max_video_duration_minutes`，视频/GIF 能读取到时长且超过配置上限时会跳过下载并保留原文链接；`scripts/test_video_download.py` 同步新增 `--max-duration-minutes` 验证参数。

### Changed

- 发送层拆分为平台 delivery adapters，`TweetSender` 保留公开入口并减少平台分支；后续 Telegram 等平台优化可集中在对应适配器中处理。
- 媒体抓取、xdown 解析、缓存清理和视频探测拆入 `media_support/`，`media.py` 保持旧导出兼容。
- 调度结果模型与格式化逻辑拆入 `scheduler_models.py` 和 `scheduler_formatting.py`，主调度状态机保持原行为。
- 命令处理拆入 `command_handlers/`，`main.py` 保留插件生命周期与命令注册编排。
- Nitter RSS 抓取遇到 SSL EOF、HTTP 5xx、429 等临时错误时，同一实例初次请求失败后最多再重试 1 次，仍失败则尝试下一个实例；媒体下载仍会按 5 秒间隔最多尝试 3 次。
- 转发过滤后如果当前 RSS 页全被过滤且存在下一页游标，会继续翻页查找更旧原创；如果 RSS 可用但全是转发，会返回空列表而不是把账号标记为抓取失败。

### Fixed

- 修复自定义 QQ/OneBot 平台实例 ID（例如 `cat:FriendMessage:*`、`cat:GroupMessage:*`）无法稳定识别真实平台类型的问题。
- 修复 QQ 合并转发视频节点和直接视频发送路径的多个兼容问题，避免纯视频节点、重复降级提示或错误回退导致的异常推送。
- 收敛媒体大小限制等重复日志，减少后台出现重复的大段媒体处理输出。

## [0.10.0] - 2026-06-15

### Added

- 媒体设置新增 `video_resolution_preference`，视频/GIF 有多个分辨率时默认只下载最高分辨率，也可指定 `lowest`、`1280p`、`852p`、`568p` 等目标分辨率。
- 后台推送和暂存发布新增批次概览：本轮第一条普通推送或合并转发头部会显示来源分组、总新推文数和各账号条数。
- `scripts/test_video_download.py` 新增 `--resolution` 参数，用于本地验证指定分辨率的视频下载结果。

### Changed

- 将旧版顶层订阅配置迁移到 `tweet_groups` 里的 `default` 默认分组；新用户未创建分组时保持用户分组为空。
- AstrBot 配置界面将 `tweet_groups` 展示为“用户分组”，旧版 `watch_users` / `push_targets` 字段改为隐藏兼容字段。
- 精简用户分组配置：分组只显示关注账号、推送目标、间隔检查开关、每日检查时间和暂存开关；拉取条数、发送间隔、启动检查、无更新提示、暂存发布时间等细节统一由全局配置控制。
- 新建默认分组使用 `default`；旧配置中已有的显式 `global` 分组 ID 会保留，`global` / `全局` 仍可作为默认分组的旧别名用于命令查找。
- `/订阅导入` 不再限制单次最多 50 个账号；仍只接受 `用户名` 或 `@用户名`，大量导入时建议分批操作以降低配置保存和同步压力。
- 后台检查设置文案调整为“后台检查”，明确总开关会控制各用户分组里的间隔检查和每日检查时间。

### Fixed

- 修复旧版顶层订阅配置迁移后，AstrBot WebUI 中默认分组缺少模板标记导致显示“找不到对应模板，请删除后重新添加”的问题。
- 修复 xdown 解析视频推文时把所有分辨率 MP4 都下载、并把视频封面 JPG 当普通图片下载的问题；检测到视频/GIF 时会忽略该推文解析出的图片附件。

## [0.9.1] - 2026-06-09

### Fixed

- 媒体缓存目录从插件源码目录迁移到 AstrBot 插件数据目录，避免本地图片/视频缓存导致 Windows 重装或删除插件目录时提示文件夹被占用。
- `/推文缓存清理` 现在会同时尝试清理当前插件数据目录缓存和旧版源码目录 `cache/` 根目录下的普通缓存文件；暂存队列媒体目录仍会保留。
- 旧版 KV seen 数据迁移到 SQLite 后会自动删除，避免卸载时勾选删除持久化数据后，重装又从旧 KV 恢复旧的“已记录账号”。
- 定时检查结果会在 seen 写入后刷新去重状态统计，并调整状态文案，避免误解为订阅账号数或推文数量。
- 手动 `/推文检查` 对全局分组改为按顶层 `push_targets` 判断当前对话归属；当前对话在全局推送目标中时，即使后台定时总开关关闭也可手动检查，未配置到任何目标时不再兜底执行。

### Added

- 新增管理员命令 `/推文记录清理 确认`，可清理全部分组的 seen 索引并删除旧版 KV seen；也支持 `/推文记录清理 分组名 确认` 只清指定分组的 SQLite seen。
- 新增管理员命令 `/订阅删除 账号1,账号2 [分组名]`，可从全局或指定分组删除订阅账号。

### Changed

- 订阅管理命令统一为 `/订阅列表`、`/订阅导入`、`/订阅删除`、`/订阅导出`、`/订阅去重`；不再注册 `/推文订阅列表`、`/推文订阅去重`、`/博主导出`。
- 暂存队列状态文案从“暂存博主”调整为“暂存账号”。

## [0.9.0] - 2026-06-09

### Added

- 新增即时普通推送流程：后台定时检查发现新推文后，普通目标会在单条推文完成翻译、媒体下载、AI 识图和 AI 评论后立即发送；QQ/`aiocqhttp` 合并转发目标仍按阈值缓冲到本轮最后统一发送。
- 手动 `/推文` 普通发送也改为单条处理完即发，并在单条标题中显示“本次结果 x/总数”。
- `/推文检查` 改为当前会话作用域：不带分组名时按当前会话 UMO 反查唯一已启用分组；带分组名时也必须当前会话属于该分组 `push_targets` 才执行。
- `/推文检查` 在开启暂存发布的分组中会临时绕过暂存队列，只向当前会话即时发送本次新推文；结果末尾追加当前分组暂存状态、待发布数量、失败重试数量、暂存博主、下次发布时间，并提示 `/推文队列`、`/推文发布`。
- 定时推送、暂存发布和即时推送的推文正文新增来源分组显示，便于同一目标会话同时属于多个分组时区分消息来源。
- AstrBot 设置界面改为按“基础、媒体、AI 翻译、AI 评论、AI 识图、定时检查、暂存发布、推送目标与分组、并发与限流”分组展示；保留旧扁平配置兼容迁移。
- 新增单条 AI 处理摘要日志，把翻译、媒体、识图、评论结果合并为一条 `AI processed` 日志；批量处理和推送日志增加 `progress=x/总数`。

### Changed

- `/推文` 和 `/镜像测试` 的开始提示从“最近 N 条推文”改为“最近最多 N 条推文”，避免与实际返回数量或单条即时发送标题混淆。
- 移除所有命令别名，只保留 README 中列出的主命令入口。
- `/镜像测试` 的镜像站参数改为必须填写完整 `http://` 或 `https://` 地址，不再接受裸域名。
- AI 评论现在必须在翻译或识图实际产生结果后才会触发；翻译和识图都跳过时不再仅凭原文生成评论。
- AI 识图结果不再单独显示在推文消息中，只作为 AI 评论上下文使用。
- 单条即时处理时，翻译、识图、评论的普通成功/跳过日志收敛为一条摘要；媒体清理纯成功日志降为 debug，失败仍保留 warning。
- 飞书/Lark 手动即时发送的 post 标题会使用“本次结果 x/总数”等手动标题覆盖。

### Fixed

- 修复即时普通推送绕过 `send_user_interval` 的问题；第二个及后续即时 batch 发送前会按配置等待。
- 修复推送统计按 `username + new_count` 合并导致不同批次误合并的问题，统计改为按内部 batch key 合并。
- 修复即时发送异常或取消时媒体没有可靠清理的问题；没有缓冲目标时发送结束或异常都会清理媒体，有缓冲 QQ 目标时异常路径也会清理当前 batch。
- 修复手动 `/推文检查` 在暂存分组中把当前会话检查产生的新推文写入全局 pending queue，导致后续 `/推文发布` 或定时暂存发布发到原配置目标的问题。
- 修复 AI 全关时摘要日志显示 `vision=unknown`、`comment=unknown` 的问题，现在显示 `off`。
- 修复无 `status_id` 的单条即时推文在全局进度下无法匹配翻译报告，摘要显示 `translation=unknown` 的问题。
- 修复飞书/Lark 手动即时发送卡片标题仍显示“最近 1 条推文”的问题。

## [0.8.0] - 2026-06-08

### Added

- 新增 SQLite 存储后端，替代 KV 存储用于分组配置和已见推文 ID 跟踪，支持 500+ 博主订阅。
- `storage_backend` 保留为兼容配置项，但运行期固定使用 `sqlite`；旧 KV 仅作为 seen 数据启动迁移来源。
- 新增 `/订阅导入` 管理命令，账号之间只用英文逗号分隔；不带分组时写入全局 `watch_users`，也可在账号列表后用空格追加分组名称、分组 ID 或别名写入对应自定义分组；只接受 `用户名` 或 `@用户名`，单次最多 50 个。
- 数据库文件存放在 AstrBot 插件数据目录：`data/nitter_tweets.db`。
- SQLite 表结构：`meta`（schema version、迁移标记）、`groups`（分组配置）、`group_users`（订阅账号）、`group_targets`（推送目标）、`seen_tweets`（已见推文）、`pending_tweets`（待推队列）、`pending_media`（暂存媒体）。
- 自动从 KV 存储迁移已见推文数据到 SQLite，迁移使用事务保证原子性，失败可重试。
- 配置分组自动同步到数据库，使用指纹追踪避免重复更新。
- 新增 `.gitignore` 规则，忽略 `*.db`、`*.db-wal`、`*.db-shm` 文件。
- 新增管理员命令 `/推文缓存清理`，用于一键清理普通图片/视频缓存文件，并保留暂存队列媒体目录。
- 新增暂存定时发布功能：可按分组把检查到的新推文写入 SQLite 队列，到配置时间统一发布。
- 新增管理员命令 `/推文队列` 和 `/推文发布`，用于查看暂存队列和手动立即发布。
- 新增 `cache/staged/` 暂存媒体目录，暂存入队时可预下载图片/视频，发布成功后自动删除。

### Changed

- 存储层升级为主版本变更（0.7.0 → 0.8.0）。
- 已见推文 ID 按 `group_id + username` 独立存储和查询，不再整组读写。
- 每个账号只维护最近 300 条 seen ID（`SEEN_LIMIT_PER_USER`），自动清理旧记录。
- 取消订阅的账号不会立即删除推送记录；超过 30 天仍未重新订阅的孤儿推送记录会在配置同步时清理。
- `/推文状态` 改为先显示全部分组项合计，再分别显示默认分组和自定义分组详情，避免把 `global` 分组误读为总数。
- `/推文状态` 和 `/推文检查` 的账号、目标等长列表最多显示 10 项，并追加“还有 N 个”提示。
- SQLite 连接允许跨 `asyncio.to_thread()` 线程串行访问，避免 AstrBot 调度器启动和 `/推文状态` 查询时触发 `SQLite objects created in a thread can only be used in that same thread`。
- 调度器初始化时执行迁移和配置同步，失败时记录错误并在 5 分钟后重试。
- 存储运行后端固定为 SQLite；即使旧配置仍写着 `kv_legacy`，也会改用 SQLite 并只从旧 KV 导入 seen 数据。
- `pending_tweets` 从占位表升级为实际发送队列，并新增 `pending_media` 记录暂存媒体索引和路径。

### Fixed

- 修复 SQLite 线程安全问题，使用 asyncio 锁保护数据库连接。
- 修复 SQL DELETE 查询逻辑错误，正确保留最新的 N 条记录。
- 修复配置指纹碰撞风险，从 64 位增加到 128 位。
- 修复资源泄漏，调度器停止时正确关闭数据库连接。
- 修复事务失效问题，移除 autocommit 模式以支持回滚。
- 配置关闭视频/GIF 附件时，跳过视频的日志从 warning 降为 info。
- `media_cache_retention_days=0` 改为本轮发送流程结束后立即删除已下载媒体文件。

### Technical

- 所有 SQLite 操作通过 `asyncio.to_thread()` 包装，避免阻塞事件循环。
- 数据库初始化时执行完整性检查（`PRAGMA integrity_check`）。
- 旧 KV 数据永不自动删除，仅作为 SQLite 启动迁移来源保留。

## [0.7.0] - 2026-06-08

### Added

- 新增插件配置里的 `tweet_groups` 自定义推文分组列表，可在 AstrBot 后台手动配置每个分组的关注账号、推送目标、间隔检查、每日定点、拉取条数和发送间隔。
- `/推文状态` 现在会显示全局分组和自定义分组总数、启用数量，并展开每个分组的订阅账号、推送目标和定时策略。

### Changed

- 定时调度启动日志改为按所有分组统计账号数和推送目标数，不再只统计全局 `watch_users` / `push_targets`。

### Fixed

- QQ/`aiocqhttp` 合并转发在同账号一次发送较多推文时按每批最多 8 条自动分批，降低大合并包在平台侧漏节点的风险。

## [0.6.10] - 2026-06-08

### Added

- 新增定时检查运行时分组模型，现有 `watch_users`、`push_targets` 和定时配置会作为 `global` 全局分组运行，为后续自定义账号分组打基础。
- `/推文检查` 支持可选分组名参数；当前版本可使用 `全局`、`默认`、`default` 检查全局分组。

### Changed

- 已见推文 ID 存储升级为按分组隔离，并兼容旧的按账号 KV 数据到 `global` 分组。

## [0.6.9] - 2026-06-07

### Changed

- 飞书/Lark 不再进入合并调度；逐账号普通发送时优先使用原生 `post` 将正文和本地图片放在同一条消息中，失败时降级到原有 `text` 正文加普通媒体附件路径。
- `merge_tweet_threshold` 收敛为 QQ/`aiocqhttp` 专用合并转发阈值；飞书/Lark、Telegram、微信 OC 和其他平台不再随阈值进入合并调度，始终逐账号普通发送。
- README、配置提示和插件元数据同步说明飞书/Lark 不支持 QQ 式合并转发，飞书单账号消息仅作为平台原生展示优化。

## [0.6.8] - 2026-06-07

### Added

- 新增 `/镜像测试 [用户名] [数量] 镜像站` 命令，用于临时测试单个 Nitter 镜像站；默认测试 `nasa`，默认获取 `1` 条，不写入全局 `instances` 配置。

### Changed

- `/推文` 恢复为只接收 `用户名 [数量]`，镜像站测试统一使用独立的 `/镜像测试` 命令。
- 移除插件命令的英文别名，README 不再展示英文命令入口。

## [0.6.7] - 2026-06-07

### Added

- 新增临时 Nitter 镜像站拉取能力，作为镜像测试命令的底层实现。

### Fixed

- 修复飞书群聊中命令末尾 At 机器人时，机器人 At 被误当作数量参数导致默认数量不生效的问题。

## [0.6.6] - 2026-06-07

### Added

- 飞书/Lark 达到 `merge_tweet_threshold` 合并阈值时，会优先使用原生 `post` 富文本发送合并正文，图片/视频附件继续按普通附件发送；`post` 失败时降级到原有 `text` 正文发送路径。

### Changed

- 手动 `/推文` 和定时推送共用合并阈值逻辑：QQ/`aiocqhttp` 继续使用 OneBot `Node/Nodes` 合并转发，飞书/Lark 使用平台专用 `post` 合并正文，其他平台继续普通消息链。

## [0.6.5] - 2026-06-07

### Fixed

- 修复飞书等平台输入 `/推文 用户名` 未传数量时可能被命令参数解析拦截的问题；数量参数现在由插件手动解析，未传时使用 `default_limit`。

## [0.6.4] - 2026-06-07

### Changed

- 将合并发送配置从布尔开关改为 `merge_tweet_threshold` 数量阈值；手动查询和定时推送的新推文总数达到阈值时才启用合并发送，`0` 表示关闭。
- `/推文状态`、定时检查摘要和启动日志改为显示合并阈值，不再显示旧的合并推送开关。

## [0.6.3] - 2026-06-07

### Fixed

- 修复飞书/Lark 平台可能只发送图片、正文文本丢失的问题；飞书现在优先通过原生 `text` 消息发送正文，再发送图片/视频附件。
- 修复飞书/Lark 定时推送和合并推送目标通过 UMO 主动发送时没有走飞书原生文本发送路径的问题。

### Changed

- README、配置提示和插件元数据补充飞书/Lark 推送目标示例与平台发送差异说明。
- 插件元数据按 AstrBot `support_platforms` 字段声明实际适配器；定时推送发送状态异常只记录短日志，不再向推送目标发送额外提示消息。
- 微信平台支持声明收窄为已测试的 `weixin_oc`，不再声明未验证的公众号或企业微信适配器。

## [0.6.2] - 2026-06-06

### Fixed

- 修复 OneBot/QQ 合并转发已送达但回包超时或网络状态不确定时，插件误判失败并发送去视频版或纯文本 fallback，导致同一轮重复推送的问题。
- 修复手动 `/推文` 在同类不确定送达异常下仍可能触发降级重发的问题。
- 修复后台定时检查与手动 `/推文检查` 并发时可能在已见 ID 写入前重复推送同一批新推文的问题。

### Changed

- 定时推送遇到不确定送达时会按可能已送达计入成功，跳过降级重试。
- 插件行为展示中的指令说明改为中文，并为 AstrBot 加载完成钩子补充描述。

## [0.6.1] - 2026-06-05

### Fixed

- 修复 Telegram、微信等非 OneBot 平台因合并转发节点不受支持导致不推送的问题；非 QQ 平台会自动使用普通消息链。
- 修复手动 `/推文` 发送路径在部分平台无支持消息段时没有明确失败提示的问题。
- 修复 Nitter RSS 将 YouTube 链接改写为 `piped.video` 后在正文或译文中重复出现的问题；现在仅将 Piped 链接还原为 `youtu.be`，其他外链保留在原文位置。
- 修复 URL 提取误识别邮箱域名的问题。
- 修复 Linux 环境下本地媒体文件 URI 多出斜杠，导致 OneBot 可能收到 `//AstrBot/...` 路径的问题。
- 修复视频推文的封面图可能被当作普通图片一起发送的问题。

### Added

- 新增 `send_image_attachments` 和 `send_video_attachments` 配置；配置界面改为只展示“发送图片附件”和“发送视频/GIF 附件”两个开关。
- `send_video_attachments` 默认关闭；关闭时保留原帖链接并提示打开原文查看。

### Changed

- 定时推送的合并转发仅 OneBot v11/`aiocqhttp` 使用；其他平台发送普通消息链。
- 翻译输入改为去除 URL 后的正文，避免译文重复输出链接。
- `push_targets` 配置说明改为推荐使用 `/sid` 获取完整 UMO；配置界面不再展示 `platform_id`。
- 精简 README，并补充多镜像重试、手动检查、多目标推送、平台发送差异和 AI 协助说明。
- 移除配置界面中的 `download_media`、`download_images`、`download_videos`，升级后以新的图片/视频发送开关为准。

## [0.6.0] - 2026-05-23

### Added

- 新增 `media_cache_retention_days` 配置，默认自动清理超过 3 天的媒体缓存。
- 新增 `vision_max_images` 配置（替代原 `vision_first_image_only`），支持每条推文识别多张图片，识图并发执行（asyncio.gather）。
- 发送合并转发消息失败时，自动去掉视频重试一次；视频位置替换为文本链接。
- 媒体类型检测增加 URL 扩展名兜底，解决 xdown 返回链接文字不含关键词时无法识别类型的问题。

### Changed

- 重构代码结构：`models.py` 合入 `utils.py`，`translator.py` 合入 `enricher.py`，`nitter_client.py` 合入 `media.py`，文件从 9 个精简为 6 个。
- 重写 enricher 模块，识图与评论的 provider 解析统一为单一 `_resolve_provider` 方法。
- 统一日志前缀为 `[NitterTweets]`。

## [0.5.0] - 2026-05-21

### Added

- 新增 AI 评论功能，可按概率调用文本大模型，为推文追加简短中文点评。
- 新增 AI 识图功能，可按概率调用视觉模型识别推文第一张图片，并把描述追加到推文节点。
- 新增 `comment_provider_id`、`vision_provider_id`、概率和提示词配置，默认关闭以避免升级后自动产生模型调用费用。
- 新增管理员命令 `/推文订阅列表`，用于查看当前有效订阅作者、重复项和无效项统计。

### Changed

- 定时调度默认不再在插件重启后立即检查新推文；如需旧行为可开启 `check_on_startup`。
- 手动查询和定时推送都会在媒体下载后执行 AI 增强；模型调用失败时保留原推文、链接和媒体发送。

## [0.4.4] - 2026-05-19

### Added

- 新增 `merge_scheduled_updates` 配置，可将一次定时检查中所有账号的新推文合并为一条聊天记录发送。
- 新增管理员命令 `/推文订阅去重`，支持规范化并去重 `watch_users`，移除重复作者和无效条目后保存配置。

### Changed

- `/推文状态` 现在会显示合并推送开关，以及订阅作者配置项、重复项和无效项统计。

## [0.4.3] - 2026-05-16

### Added

- 新增 `notify_no_updates` 配置，可在定时检查无新推文或首次记录账号时发送检查摘要。
- 新增管理员诊断命令 `/推文状态`、`/推文检查`，支持查看调度器状态并手动触发一次定时检查。

### Changed

- 定时检查改为输出更清晰的 info 级别日志，包含触发原因、关注账号、推送目标、无更新、初始化和失败统计。
- 调度器增加启动兜底，在插件初始化和 AstrBot 加载完成时都会尝试确保任务运行。

## [0.4.2] - 2026-05-15

### Added

- 增加翻译链路日志，可看到翻译是否开启、每条推文是否触发、跳过原因、provider 来源和模型返回结果。
- 翻译 provider 增加兜底逻辑：未配置模型且当前会话无默认模型时，尝试使用 AstrBot 已配置 provider 列表中的第一个。

## [0.4.1] - 2026-05-15

### Changed

- 简化翻译提示词配置：将原来的系统提示词和用户提示词模板合并为一个 `translate_prompt`。
- 翻译调用改为只向 `llm_generate` 传入一份 prompt，配置界面更清爽。

### Docs

- README 增加访问计数图片。

## [0.4.0] - 2026-05-15

### Added

- 新增非中文推文翻译功能，可调用 AstrBot 已配置的大模型生成简体中文译文。
- 新增 `translation_provider_id`，支持通过 WebUI 选择已有模型 provider。
- 新增 `translator.py`，将语言检测和翻译调用从抓取、发送逻辑中拆出。
- 手动查询和定时推送均支持在发送前追加译文。

## [0.3.0] - 2026-05-15

### Added

- 新增定时检查与推送功能。
- 支持关注多个用户并按间隔或每日定点检查新推文。
- 支持 `push_targets` 配置多个推送目标。
- 首次启用账号时只记录当前已见推文 ID，不推送历史内容。
- 新增 KV 已见状态存储，每个账号保留最近 100 个已见推文 ID。

## [0.2.0] - 2026-05-15

### Added

- 新增推文媒体解析与下载。
- 将 Nitter 推文链接转换为 `x.com/<user>/status/<id>` 后传入 xdown 解析。
- 支持下载图片、视频和 GIF，并作为合并转发消息段发送。
- 媒体解析或下载失败时保留文本和链接发送。

## [0.1.0] - 2026-05-15

### Added

- 初始版本。
- 支持通过 Nitter RSS 获取指定用户最近公开推文。
- 支持 `/推文` 命令。
- 支持以聊天记录/合并转发形式发送推文。
