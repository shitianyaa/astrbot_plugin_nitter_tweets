{
  "basic": {
    "description": "基础设置",
    "type": "object",
    "hint": "Nitter 请求、存储和手动查询的基础配置。",
    "items": {
      "storage_backend": {
        "description": "存储后端",
        "type": "string",
        "default": "sqlite",
        "hint": "运行期固定使用本地 SQLite 数据库存储分组配置和推送记录，支持 500+ 账号订阅；旧 KV 数据仅在启动迁移时自动导入，不再作为运行后端。"
      },
      "instances": {
        "description": "Nitter 实例列表",
        "type": "list",
        "default": [
          "https://nitter.net"
        ],
        "hint": "按顺序尝试 /<username>/rss。公共实例不适合高频抓取，长期使用建议改为自建实例。"
      },
      "default_limit": {
        "description": "手动查询默认条数",
        "type": "int",
        "default": 5,
        "hint": "用于 /推文 用户名 和 /镜像测试 [用户名] 镜像站URL 未填写数量时的默认获取条数；用户填写数量时不会被此项截断。为避免刷屏和公共 Nitter 实例压力，建议日常不要一次请求过多。"
      },
      "request_timeout": {
        "description": "单个 Nitter 实例超时秒数",
        "type": "float",
        "default": 12.0,
        "hint": "单次 RSS 请求等待该实例响应的最长秒数；同一实例初次请求失败后最多再重试 1 次，仍失败才尝试下一个实例。"
      },
      "cooldown_seconds": {
        "description": "用户请求冷却秒数",
        "type": "float",
        "default": 15.0,
        "hint": "同一会话同一用户的命令冷却，降低公共实例压力。设为 0 可关闭。"
      },
      "user_agent": {
        "description": "Nitter 请求 User-Agent",
        "type": "string",
        "default": "Mozilla/5.0 (compatible; AstrBotNitterTweets/0.4)",
        "hint": "发送给 Nitter 实例的 User-Agent。"
      },
      "filter_reposts_enabled": {
        "description": "过滤博主转发",
        "type": "bool",
        "default": true,
        "hint": "开启后会过滤订阅账号转发他人的推文；博主自己发布的引用或评论推文仍会保留。老配置缺少该项时按开启处理。"
      }
    }
  },
  "media": {
    "description": "媒体设置",
    "type": "object",
    "hint": "控制推文图片、视频/GIF 附件下载、缓存和发送行为。",
    "items": {
      "send_image_attachments": {
        "description": "发送图片附件",
        "type": "bool",
        "default": true,
        "hint": "开启后会解析推文图片，下载到本地缓存后随消息发送；QQ/OneBot 会把图片拆成独立图片消息或合并转发节点，其他平台保持图文同消息。关闭后只保留正文和原帖链接。"
      },
      "send_video_attachments": {
        "description": "发送视频/GIF 附件",
        "type": "bool",
        "default": false,
        "hint": "视频/GIF 附件发送目前不太成熟，还在优化中；容易受平台大小、格式、CDN 上传和本地文件权限影响。默认关闭，仅保留原帖链接。检测到视频/GIF 时会忽略图片附件，包括视频封面。"
      },
      "video_resolution_preference": {
        "description": "视频分辨率偏好",
        "type": "string",
        "default": "highest",
        "hint": "视频/GIF 有多个分辨率时只下载一个。填写 highest 使用最高分辨率，lowest 使用最低分辨率，也可填写 1280p、852p、568p 等目标分辨率；目标不存在时选择不超过目标的最接近分辨率。"
      },
      "max_video_duration_minutes": {
        "description": "视频最长下载分钟数",
        "type": "float",
        "default": 8.0,
        "hint": "检测到视频/GIF 时会尽量读取时长；超过该分钟数则跳过下载并保留原文链接。范围 1-8 分钟，读不到时长时不会拦截，仍按大小上限处理。"
      },
      "max_media_per_tweet": {
        "description": "单条推文最大媒体数",
        "type": "int",
        "default": 4,
        "hint": "限制每条推文最多下载并发送多少个媒体，范围 0-12。"
      },
      "media_timeout": {
        "description": "媒体解析/下载超时秒数",
        "type": "float",
        "default": 25.0
      },
      "media_max_size_mb": {
        "description": "单个媒体大小上限 MB",
        "type": "float",
        "default": 25.0,
        "hint": "超过上限的媒体会跳过，避免大文件拖慢机器人。"
      },
      "xdown_api_url": {
        "description": "xdown 解析 API",
        "type": "string",
        "default": "https://xdown.app/api/ajaxSearch",
        "hint": "参考 astrbot_plugin_parser-main 的 TwitterParser。"
      },
      "media_user_agent": {
        "description": "媒体解析/下载 User-Agent",
        "type": "string",
        "default": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36"
      }
    }
  },
  "ai_translation": {
    "description": "AI 翻译",
    "type": "object",
    "hint": "控制推文翻译开关、服务商和提示词。",
    "items": {
      "translate_enabled": {
        "description": "翻译非中文推文",
        "type": "bool",
        "default": false,
        "hint": "开启后会检测推文文本是否不像中文，并调用大模型生成简体中文译文。"
      },
      "translation_provider_id": {
        "description": "翻译使用的模型",
        "type": "string",
        "_special": "select_provider",
        "default": "",
        "hint": "留空时，手动命令使用当前会话模型；定时推送会尝试使用第一个推送目标的默认模型。若定时翻译不可用，建议在这里指定模型。"
      },
      "translate_min_chars": {
        "description": "翻译最短文本长度",
        "type": "int",
        "default": 8,
        "hint": "清理链接和 @ 后，少于该长度的推文不会翻译，范围 0-1000。"
      },
      "translate_max_chars": {
        "description": "单条翻译最大字符数",
        "type": "int",
        "default": 2000,
        "hint": "发送给模型的单条推文最大长度，范围 100-10000。"
      },
      "translate_chinese_ratio_threshold": {
        "description": "中文占比阈值",
        "type": "float",
        "default": 0.2,
        "hint": "中文字符占比低于该值时判定为非中文；日文假名、韩文会直接判定为需要翻译。范围 0-1。"
      },
      "translate_prompt": {
        "description": "翻译提示词",
        "type": "text",
        "default": "你是专业翻译助手。请把下面这条推文翻译成自然简体中文。保留人名、账号、标签和原有换行；不要添加解释，不要输出原文。\n\n{text}",
        "hint": "必须包含 {text}，插件会把去除 URL 后的推文正文替换进去；链接仍保留在原文区。"
      }
    }
  },
  "schedule": {
    "description": "后台检查",
    "type": "object",
    "hint": "控制后台检查总开关、全局间隔、启动检查、无更新提示和每次拉取数量；分组里的间隔检查开关和每日检查时间只有在此处总开关开启后才会生效。",
    "items": {
      "schedule_enabled": {
        "description": "启用后台检查",
        "type": "bool",
        "default": false,
        "hint": "开启后，插件才会按用户分组配置执行间隔检查或每日检查，并推送到对应分组的推送目标；关闭时分组里的间隔检查开关和每日检查时间均不会触发。"
      },
      "notify_no_updates": {
        "description": "后台检查无新推文时发送检查结果提示",
        "type": "bool",
        "default": false,
        "hint": "关闭时只有发现新推文或失败时写日志；开启后，本次无更新或首次记录账号时也会向对应分组的推送目标发送检查摘要。"
      },
      "check_on_startup": {
        "description": "启动后立即检查",
        "type": "bool",
        "default": false,
        "hint": "关闭时插件重启后不会立刻检查新推文，而是等到下一个间隔或每日定点；只有后台检查总开关开启时才会生效。"
      },
      "check_interval_minutes": {
        "description": "全局间隔检查分钟数",
        "type": "int",
        "default": 30,
        "hint": "这里只设置间隔时间，不是间隔检查总开关；后台检查总开关开启后，启用间隔检查的用户分组都会按这个全局间隔运行，范围 1-1440。"
      },
      "scheduled_fetch_limit": {
        "description": "后台检查每账号拉取条数",
        "type": "int",
        "default": 5,
        "hint": "每次每个账号从 RSS 拉取最近多少条用于对比新推文，范围 1-20。"
      }
    }
  },
  "push": {
    "description": "推送目标与用户分组",
    "type": "object",
    "hint": "配置 QQ 合并阈值、发送间隔和用户分组；关注账号和推送目标请在用户分组中填写。",
    "items": {
      "merge_tweet_threshold": {
        "description": "QQ 合并推送触发条数",
        "type": "int",
        "default": 2,
        "hint": "QQ/aiocqhttp 目标在本次发送的新推文总数达到该值时启用 OneBot v11 Node/Nodes 合并转发；0 表示关闭，1 表示有新推文就合并，默认 2 表示 2 条及以上才合并。飞书/Lark、Telegram、微信 OC 和其他平台不受该阈值影响，始终使用普通逐账号发送。范围 0-20。"
      },
      "send_target_interval": {
        "description": "多目标发送间隔秒数",
        "type": "float",
        "default": 1.5,
        "hint": "同一账号的新推文发送到多个目标时，目标之间的等待时间，范围 0-60。"
      },
      "send_user_interval": {
        "description": "多账号发送间隔秒数",
        "type": "float",
        "default": 2.0,
        "hint": "多个关注账号都有新推文时，账号之间的等待时间，范围 0-60。"
      },
      "watch_users": {
        "description": "旧版关注账号列表",
        "type": "list",
        "default": [],
        "hint": "旧版兼容字段。启动后会迁移到用户分组里的默认分组；新配置请在用户分组中填写关注账号。",
        "invisible": true
      },
      "push_targets": {
        "description": "旧版定时推送目标列表",
        "type": "list",
        "default": [],
        "hint": "旧版兼容字段。启动后会迁移到用户分组里的默认分组；新配置请在用户分组中填写推送目标。",
        "invisible": true
      },
      "tweet_groups": {
        "description": "用户分组",
        "type": "template_list",
        "default": [],
        "hint": "每个分组只配置关注账号、推送目标、是否参与间隔检查和每日检查时间；拉取数量、发送间隔等细节使用全局配置。分组里的间隔检查和每日检查时间只有在后台检查总开关开启后才会触发。没有创建分组时保持为空；旧版关注账号和推送目标会自动迁移到默认分组。分组之间的配置和推送记录互相隔离。",
        "templates": {
          "group": {
            "name": "用户分组",
            "display_item": "name",
            "hide_hint_in_list": true,
            "items": {
              "name": {
                "description": "分组名称",
                "type": "string",
                "default": "",
                "hint": "用于 /推文检查 分组名 和 /推文状态 展示，例如 科技、新闻、NASA。"
              },
              "group_id": {
                "description": "内部稳定分组 ID",
                "type": "string",
                "default": "",
                "hint": "内部存储键，启动时自动补齐；已有值会保留。请不要手动修改，否则可能导致推送记录和历史记录断开。",
                "invisible": true
              },
              "enabled": {
                "description": "启用分组",
                "type": "bool",
                "default": true
              },
              "watch_users": {
                "description": "分组关注账号",
                "type": "list",
                "default": [],
                "hint": "每行一个账号，支持 NASA、@NASA、https://x.com/NASA。首次启用某个账号只记录当前最新推文，不推送历史内容。"
              },
              "push_targets": {
                "description": "分组推送目标",
                "type": "list",
                "default": [],
                "hint": "每行一个 UMO。在目标群聊或私聊发送 /sid 后复制返回值，例如 aiocqhttp:GroupMessage:123456、lark:GroupMessage:oc_xxx。"
              },
              "interval_check_enabled": {
                "description": "启用间隔检查",
                "type": "bool",
                "default": true,
                "hint": "开启后，且后台检查总开关开启时，该分组按全局间隔检查分钟数运行。"
              },
              "daily_check_times": {
                "description": "每日检查时间列表",
                "type": "list",
                "default": [],
                "hint": "格式 HH:MM，每行一个；后台检查总开关开启后才会触发；留空表示该分组不使用每日定点检查。"
              },
              "filter_plain_text_enabled": {
                "description": "过滤纯文本推文",
                "type": "bool",
                "default": false,
                "hint": "开启后，该分组的定时推送会跳过没有任何作者上传媒体附件(图片/视频/GIF)的推文；只在 RSS 源头判断，不依赖媒体下载，链接预览卡片图和 Twitter Article（长文）封面图不算作者上传媒体。手动 /推文、/镜像测试 命令不受影响，仍返回全部。"
              },
              "aliases": {
                "description": "分组别名",
                "type": "list",
                "default": [],
                "hint": "旧版兼容字段。可继续用于 /推文检查，但不再建议在配置界面维护。",
                "invisible": true
              },
              "check_on_startup": {
                "description": "启动后立即检查",
                "type": "bool",
                "default": false,
                "invisible": true
              },
              "notify_no_updates": {
                "description": "无新推文时发送检查结果提示",
                "type": "bool",
                "default": false,
                "invisible": true
              },
              "check_interval_minutes": {
                "description": "间隔检查分钟数",
                "type": "int",
                "default": 30,
                "invisible": true
              },
              "daily_check_enabled": {
                "description": "启用每日定点检查",
                "type": "bool",
                "default": true,
                "invisible": true
              },
              "scheduled_fetch_limit": {
                "description": "每账号拉取条数",
                "type": "int",
                "default": 5,
                "invisible": true
              },
              "send_target_interval": {
                "description": "多目标发送间隔秒数",
                "type": "float",
                "default": 1.5,
                "invisible": true
              },
              "send_user_interval": {
                "description": "多账号发送间隔秒数",
                "type": "float",
                "default": 2.0,
                "invisible": true
              }
            }
          }
        }
      }
    }
  },
  "logging": {
    "description": "日志设置",
    "type": "object",
    "hint": "控制插件写入 AstrBot 后台日志的详细程度；不影响群聊/私聊中的推送内容和命令返回。",
    "items": {
      "brief_log_enabled": {
        "description": "后台日志简略模式",
        "type": "bool",
        "default": true,
        "hint": "开启后只保留每轮检查的结果摘要、失败详情、推送成功率和关键 warning/error；关闭后输出详细处理过程日志。老配置缺少该项时按开启处理。"
      }
    }
  },
  "performance": {
    "description": "并发与限流",
    "type": "object",
    "hint": "控制后台检查的并发抓取和并发准备。默认关闭；建议只在自建 Nitter 镜像和模型额度充足时开启。",
    "items": {
      "concurrent_fetch_enabled": {
        "description": "启用后台并发拉取",
        "type": "bool",
        "default": false,
        "hint": "开启后，只有同时配置了并发专用 Nitter 镜像且并发数大于 1，后台检查才会并发拉取多个账号；否则保持旧串行行为。手动 /推文、/镜像测试 不受影响。"
      },
      "fetch_concurrency": {
        "description": "账号拉取并发数",
        "type": "int",
        "default": 3,
        "hint": "后台检查同时拉取多少个账号 RSS，范围 1-8。公共 Nitter 镜像不建议高并发；建议配合自建镜像使用。"
      },
      "concurrent_fetch_instances": {
        "description": "并发专用 Nitter 镜像",
        "type": "list",
        "default": [],
        "hint": "仅供后台并发拉取使用，可填写多个自建 Nitter 镜像。留空时不会启用并发拉取，也不会回退到基础设置里的 instances。公共镜像容易限流或拒绝访问，不建议用于高并发。"
      },
      "concurrent_prepare_enabled": {
        "description": "启用媒体和模型并发准备",
        "type": "bool",
        "default": false,
        "hint": "开启后，后台检查发现的新推文会并发执行翻译和媒体下载；发送仍按关注账号和推文顺序执行。"
      },
      "prepare_concurrency": {
        "description": "推文准备并发数",
        "type": "int",
        "default": 2,
        "hint": "后台检查同时准备多少条推文或账号批次，范围 1-8。数值越大，对 xdown、模型 provider 和本机网络压力越高。"
      }
    }
  },
  "_legacy_grouped_config_migrated": {
    "description": "Legacy grouped config migration completed",
    "type": "bool",
    "default": false,
    "invisible": true,
    "hint": "Internal migration marker. Do not edit manually."
  },
  "_default_group_config_migrated": {
    "description": "Default group config migration completed",
    "type": "bool",
    "default": false,
    "invisible": true,
    "hint": "Internal migration marker. Do not edit manually."
  },
  "_media_cache_cleanup_v16_migrated": {
    "description": "Media cache cleanup migration completed",
    "type": "bool",
    "default": false,
    "invisible": true,
    "hint": "Internal migration marker. Do not edit manually."
  },
  "_media_cache_send_delete_migrated": {
    "description": "Media cache send-delete migration completed",
    "type": "bool",
    "default": false,
    "invisible": true,
    "hint": "Internal migration marker. Do not edit manually."
  },
  "_max_video_duration_grouped_config_migrated": {
    "description": "Max video duration grouped config migration completed",
    "type": "bool",
    "default": false,
    "invisible": true,
    "hint": "Internal migration marker. Do not edit manually."
  },
  "storage_backend": {
    "description": "存储后端",
    "type": "string",
    "default": "sqlite",
    "hint": "运行期固定使用本地 SQLite 数据库存储分组配置和推送记录，支持 500+ 账号订阅；旧 KV 数据仅在启动迁移时自动导入，不再作为运行后端。",
    "invisible": true
  },
  "instances": {
    "description": "Nitter 实例列表",
    "type": "list",
    "default": [
      "https://nitter.net"
    ],
    "hint": "按顺序尝试 /<username>/rss。公共实例不适合高频抓取，长期使用建议改为自建实例。",
    "invisible": true
  },
  "default_limit": {
    "description": "手动查询默认条数",
    "type": "int",
    "default": 5,
    "hint": "用于 /推文 用户名 和 /镜像测试 [用户名] 镜像站URL 未填写数量时的默认获取条数；用户填写数量时不会被此项截断。为避免刷屏和公共 Nitter 实例压力，建议日常不要一次请求过多。",
    "invisible": true
  },
  "request_timeout": {
    "description": "单个 Nitter 实例超时秒数",
    "type": "float",
    "default": 12.0,
    "hint": "单次 RSS 请求等待该实例响应的最长秒数；同一实例初次请求失败后最多再重试 1 次，仍失败才尝试下一个实例。",
    "invisible": true
  },
  "cooldown_seconds": {
    "description": "用户请求冷却秒数",
    "type": "float",
    "default": 15.0,
    "hint": "同一会话同一用户的命令冷却，降低公共实例压力。设为 0 可关闭。",
    "invisible": true
  },
  "user_agent": {
    "description": "Nitter 请求 User-Agent",
    "type": "string",
    "default": "Mozilla/5.0 (compatible; AstrBotNitterTweets/0.4)",
    "hint": "发送给 Nitter 实例的 User-Agent。",
    "invisible": true
  },
  "send_image_attachments": {
    "description": "发送图片附件",
    "type": "bool",
    "default": true,
    "hint": "开启后会解析推文图片，下载到本地缓存后随消息发送；QQ/OneBot 会把图片拆成独立图片消息或合并转发节点，其他平台保持图文同消息。关闭后只保留正文和原帖链接。",
    "invisible": true
  },
  "send_video_attachments": {
    "description": "发送视频/GIF 附件",
    "type": "bool",
    "default": false,
    "hint": "视频/GIF 附件发送目前不太成熟，还在优化中；容易受平台大小、格式、CDN 上传和本地文件权限影响。默认关闭，仅保留原帖链接。检测到视频/GIF 时会忽略图片附件，包括视频封面。",
    "invisible": true
  },
  "video_resolution_preference": {
    "description": "视频分辨率偏好",
    "type": "string",
    "default": "highest",
    "hint": "视频/GIF 有多个分辨率时只下载一个。填写 highest 使用最高分辨率，lowest 使用最低分辨率，也可填写 1280p、852p、568p 等目标分辨率；目标不存在时选择不超过目标的最接近分辨率。",
    "invisible": true
  },
  "max_video_duration_minutes": {
    "description": "视频最长下载分钟数",
    "type": "float",
    "default": 8.0,
    "hint": "检测到视频/GIF 时会尽量读取时长；超过该分钟数则跳过下载并保留原文链接。范围 1-8 分钟，读不到时长时不会拦截，仍按大小上限处理。",
    "invisible": true
  },
  "max_media_per_tweet": {
    "description": "单条推文最大媒体数",
    "type": "int",
    "default": 4,
    "hint": "限制每条推文最多下载并发送多少个媒体，范围 0-12。",
    "invisible": true
  },
  "media_timeout": {
    "description": "媒体解析/下载超时秒数",
    "type": "float",
    "default": 25.0,
    "invisible": true
  },
  "media_max_size_mb": {
    "description": "单个媒体大小上限 MB",
    "type": "float",
    "default": 25.0,
    "hint": "超过上限的媒体会跳过，避免大文件拖慢机器人。",
    "invisible": true
  },
  "xdown_api_url": {
    "description": "xdown 解析 API",
    "type": "string",
    "default": "https://xdown.app/api/ajaxSearch",
    "hint": "参考 astrbot_plugin_parser-main 的 TwitterParser。",
    "invisible": true
  },
  "media_user_agent": {
    "description": "媒体解析/下载 User-Agent",
    "type": "string",
    "default": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36",
    "invisible": true
  },
  "translate_enabled": {
    "description": "翻译非中文推文",
    "type": "bool",
    "default": false,
    "hint": "开启后会检测推文文本是否不像中文，并调用大模型生成简体中文译文。",
    "invisible": true
  },
  "translation_provider_id": {
    "description": "翻译使用的模型",
    "type": "string",
    "_special": "select_provider",
    "default": "",
    "hint": "留空时，手动命令使用当前会话模型；定时推送会尝试使用第一个推送目标的默认模型。若定时翻译不可用，建议在这里指定模型。",
    "invisible": true
  },
  "translate_min_chars": {
    "description": "翻译最短文本长度",
    "type": "int",
    "default": 8,
    "hint": "清理链接和 @ 后，少于该长度的推文不会翻译，范围 0-1000。",
    "invisible": true
  },
  "translate_max_chars": {
    "description": "单条翻译最大字符数",
    "type": "int",
    "default": 2000,
    "hint": "发送给模型的单条推文最大长度，范围 100-10000。",
    "invisible": true
  },
  "translate_chinese_ratio_threshold": {
    "description": "中文占比阈值",
    "type": "float",
    "default": 0.2,
    "hint": "中文字符占比低于该值时判定为非中文；日文假名、韩文会直接判定为需要翻译。范围 0-1。",
    "invisible": true
  },
  "translate_prompt": {
    "description": "翻译提示词",
    "type": "text",
    "default": "你是专业翻译助手。请把下面这条推文翻译成自然简体中文。保留人名、账号、标签和原有换行；不要添加解释，不要输出原文。\n\n{text}",
    "hint": "必须包含 {text}，插件会把去除 URL 后的推文正文替换进去；链接仍保留在原文区。",
    "invisible": true
  },
  "schedule_enabled": {
    "description": "启用定时检查",
    "type": "bool",
    "default": false,
    "hint": "开启后会按用户分组配置定时检查关注账号，并推送到对应分组的推送目标。",
    "invisible": true
  },
  "notify_no_updates": {
    "description": "定时检查无新推文时发送检查结果提示",
    "type": "bool",
    "default": false,
    "hint": "关闭时只有发现新推文或失败时写日志；开启后，本次无更新或首次记录账号时也会向对应分组的推送目标发送检查摘要。",
    "invisible": true
  },
  "check_on_startup": {
    "description": "启动后立即检查",
    "type": "bool",
    "default": false,
    "hint": "关闭时插件重启后不会立刻检查新推文，而是等到下一个间隔或每日定点；开启后恢复旧行为。",
    "invisible": true
  },
  "interval_check_enabled": {
    "description": "启用间隔检查",
    "type": "bool",
    "default": true,
    "hint": "schedule_enabled 开启后生效。按 check_interval_minutes 周期检查。",
    "invisible": true
  },
  "check_interval_minutes": {
    "description": "间隔检查分钟数",
    "type": "int",
    "default": 30,
    "hint": "范围 1-1440。",
    "invisible": true
  },
  "daily_check_enabled": {
    "description": "启用每日定点检查",
    "type": "bool",
    "default": false,
    "hint": "schedule_enabled 开启后生效。可与间隔检查同时开启。",
    "invisible": true
  },
  "daily_check_times": {
    "description": "每日检查时间列表",
    "type": "list",
    "default": [
      "08:00"
    ],
    "hint": "格式 HH:MM，每行一个。",
    "invisible": true
  },
  "scheduled_fetch_limit": {
    "description": "定时检查每账号拉取条数",
    "type": "int",
    "default": 5,
    "hint": "每次每个账号从 RSS 拉取最近多少条用于对比新推文，范围 1-20。",
    "invisible": true
  },
  "concurrent_fetch_enabled": {
    "description": "启用后台并发拉取",
    "type": "bool",
    "default": false,
    "hint": "旧版兼容字段。新配置请在并发与限流中维护。",
    "invisible": true
  },
  "fetch_concurrency": {
    "description": "账号拉取并发数",
    "type": "int",
    "default": 3,
    "hint": "旧版兼容字段。新配置请在并发与限流中维护。",
    "invisible": true
  },
  "concurrent_fetch_instances": {
    "description": "并发专用 Nitter 镜像",
    "type": "list",
    "default": [],
    "hint": "旧版兼容字段。新配置请在并发与限流中维护。",
    "invisible": true
  },
  "concurrent_prepare_enabled": {
    "description": "启用媒体和模型并发准备",
    "type": "bool",
    "default": false,
    "hint": "旧版兼容字段。新配置请在并发与限流中维护。",
    "invisible": true
  },
  "prepare_concurrency": {
    "description": "推文准备并发数",
    "type": "int",
    "default": 2,
    "hint": "旧版兼容字段。新配置请在并发与限流中维护。",
    "invisible": true
  },
  "merge_tweet_threshold": {
    "description": "QQ 合并推送触发条数",
    "type": "int",
    "default": 2,
    "hint": "QQ/aiocqhttp 目标在本次发送的新推文总数达到该值时启用 OneBot v11 Node/Nodes 合并转发；0 表示关闭，1 表示有新推文就合并，默认 2 表示 2 条及以上才合并。飞书/Lark、Telegram、微信 OC 和其他平台不受该阈值影响，始终使用普通逐账号发送。范围 0-20。",
    "invisible": true
  },
  "send_target_interval": {
    "description": "多目标发送间隔秒数",
    "type": "float",
    "default": 1.5,
    "hint": "同一账号的新推文发送到多个目标时，目标之间的等待时间，范围 0-60。",
    "invisible": true
  },
  "send_user_interval": {
    "description": "多账号发送间隔秒数",
    "type": "float",
    "default": 2.0,
    "hint": "多个关注账号都有新推文时，账号之间的等待时间，范围 0-60。",
    "invisible": true
  },
  "watch_users": {
    "description": "旧版关注账号列表",
    "type": "list",
    "default": [],
    "hint": "旧版兼容字段。启动后会迁移到用户分组里的默认分组；新配置请在用户分组中填写关注账号。",
    "invisible": true
  },
  "push_targets": {
    "description": "旧版定时推送目标列表",
    "type": "list",
    "default": [],
    "hint": "旧版兼容字段。启动后会迁移到用户分组里的默认分组；新配置请在用户分组中填写推送目标。",
    "invisible": true
  },
  "tweet_groups": {
    "description": "旧版用户分组",
    "type": "list",
    "default": [],
    "hint": "Legacy compatibility field; hidden in UI and migrated into push.tweet_groups on first startup.",
    "invisible": true
  }
}
